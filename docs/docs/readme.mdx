import Readme from '../../README.md'
import { BlocksProvider, Block } from '../../index'
import Header from '../header.mdx'
import Footer from '../footer.mdx'

export default BlocksProvider

<Header />

<Block
  {...props}
  maxWidth={768}
  mx='auto'
  px={4}
  py={5}
  components={{
    pre: {
      color: 'white',
      bg: 'primary',
    },
    inlineCode: {
      color: 'highlight',
    },
  }}>
  <Readme />
</Block>

<Footer />
