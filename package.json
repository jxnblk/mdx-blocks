{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs"
  ],
  "scripts": {
    "clean": "yarn workspaces run clean && rm -rf node_modules",
    "start": "yarn workspace mdx-blocks-docs start"
  }
}
